{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "Always ask me before writing any files, editing files, or running shell commands that could change the system. Reading/searching is always fine without asking.",
    "Be concise but explain context when it matters. Don't give one-word answers to complex questions, but don't over-explain things the user already knows."
  ]
}
